# Copy to .env for local use. Do not commit .env.

# SQLite is the default because it runs without server setup.
DATABASE_URL=sqlite:///data/eth_features.sqlite
FEATURE_TABLE=ohlcv_features_1m

# Optional MySQL example:
# DATABASE_URL=mysql+mysqlconnector://user:password@localhost/eth_research

PRICE_CSV=data/sample_ethusd_1m.csv
COINBASE_PRODUCT_ID=ETH-USD

